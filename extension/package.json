{
  "name": "contentstudio-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension that collects YouTube Studio analytics and pushes them to the ContentStudio desktop app.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
