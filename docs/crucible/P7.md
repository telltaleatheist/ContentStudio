# P7: the editor's voice isolation on Crucible

Build notes for phase P7 of CRUCIBLE-MIGRATION-PLAN.md (§16), built 2026-09-25 on branch
`worktree-agent-a8b1fa6d2acd7ced6`, off `crucible` at `9ae93be` (P1 merged). LEDGER #200: voice
isolation is a Crucible `denoise` job; #202: the editor is unfrozen for this work.

## What the server said first

The Mac's `/v1/info` was read before anything was written (plan §0a: check the rows before
uploading). Crucible was 1.0.34 at the start of the build and 1.0.35 after a redeploy during it.
Its `denoise` row, verbatim in substance:

| id | installed | resident | vram_bytes | source |
|---|---|---|---|---|
| `denoise-roformer` | true | false | 2,523,710,036 | `Politrees/UVR_resources:…/denoise_mel_band_roformer_aufr33_sdr_27.9959.ckpt` |
| `vocals-roformer` | **false** | false | 2,523,719,636 | `Politrees/UVR_resources:…/vocals_mel_band_roformer.ckpt` |

`job_types` listed `denoise` (the rvc env is installed there). The vocals weights were not
pulled; `crucible denoise pull vocals-roformer` placed them (0.91 GB, both digests verified,
10 s) so the acceptance could run. That is the exact refusal the Denoise row now prints on a
server in that state.

## What was built

| piece | files |
|---|---|
| the door | `electron/crucible/denoise.ts`: `CrucibleVoiceIsolator` (start / separate / dispose), `voiceIsolationAvailability`, `readWavFormat`, `crucibleVoiceIsolation` (the editor's deps over the Crucible context), `parkOnProbe` |
| the exchange with Python | `electron/services/editor/separation-protocol.ts`; `python-service.ts` routes `separation_request` / `separation_release` to it |
| the Python side | `editor-backend/core/voice_separation.py` (`isolate_voice`, `request_separation`); `cli/electron_workflow.py` `denoise_mic_audio` calls it in-process |
| the Denoise toggle | `editor:voice-isolation-status` (editor-ipc.ts), preload, `electron.ts`, the host port's `voiceIsolationStatus()`, the adapter, the project setup modal |
| the module | `shared/crucible/contentstudio.toml` + `.module.json`: `rvc` job type and the `vocals-roformer` `denoise` subject, generated by v1.0.34's own `gen-modules.py` (`--check` clean) |
| the fake | `tools/fake-crucible.js`: a `denoise` job validated in the server's order, the two separators' catalog rows, `denoise` in the stocked job types |
| a second client | `tools/crucible-raw-denoise-client.js`: the seam over raw `fetch` |
| keeper | `tools/test-crucible-denoise.js` (19 checks) |
| live tool | `tools/crucible-denoise-live.js` |

**Only the model call moved.** Python still plans the chunks at silences, skips silent ones,
extracts each at 44.1 kHz (`pcm_s24le`, stereo), concatenates the stems and resamples to 48 kHz.
`separate_chunk` (a subprocess from `voice-separator-env`) became `request_separation`: one JSON
line on stdout, one answer on stdin. The PLAN/CHUNK progress lines are unchanged; main moves the
operation row's bar *within* a chunk from the job's own events.

### The wire, per chunk

1. `GET /v1/info` once per track: no `denoise` → `voice_isolation_not_offered`; no
   `vocals-roformer` row → `voice_isolation_model_not_offered`; `installed: false` →
   `voice_isolation_model_not_installed`, naming `crucible denoise pull vocals-roformer`. Nothing
   is uploaded before this answers.
2. The chunk's WAV header must say 44,100 Hz (`voice_isolation_input_not_44100`, before upload;
   the server never resamples and its worker would fail the job after the upload).
3. `POST /v1/uploads` (the file streamed from disk with `fs.openAsBlob`).
4. `POST /v1/jobs {type: "denoise", model: "vocals-roformer", params: {}, inputs: {<chunk name>: {blob_id}}, client_ref}`.
   `params` is `{}` because the server forbids every key (PHASE4-AUDIO.md §4.2: "`params` is
   empty, and that is the contract"). `409 env_missing` is relayed with `crucible install rvc`.
5. `409 server_busy` / `409 leased` → the injected **park** gets the holder's line
   (`CrucibleBusy.busyLine` / `CrucibleLeased.leasedLine`) and the operation row shows it. The
   blob is kept (a refused submit never consumes it). Nothing loops: the park decides when to ask
   again, and exactly one submit follows each release.
6. `GET /v1/jobs/{id}/events`: `warming` and `progress` go to the row; `failed` throws
   `VoiceIsolationFailed` with the server's code and message, which Python raises as a
   `VoiceIsolationError` and the run aborts (the noisy original never ships).
7. `GET /v1/jobs/{id}/artifacts/{done.primary_stem}`: the stem the server names (never picked by
   filename here), written beside the target and renamed, after its header is checked against the
   chunk: same rate, same frame count (the server's own invariant, asserted on arrival).
8. Cancel: the workflow ending (a cancel kills the Python process) aborts a signal; the door sends
   `DELETE /v1/jobs/{id}` and reads the stream on to its `cancelled` frame.

**The lease.** Taken after the first chunk of a track (a lease names what is resident and never
loads), heartbeated before each later chunk, released on the track's `separation_release` and
again when the run ends. Failing to take it is a declared mode, logged: the pass is correct
without it, only slower (each chunk would reload the 913 MB checkpoint). Ported from BookForge's
`electron/crucible/denoise.ts`.

## What the real client must provide

`DenoiseClient` in `electron/crucible/denoise.ts` is the whole seam. It is typed as the vendored
SDK (`@crucible/client` 1.0.34) spells its calls, so the SDK's `CrucibleClient` satisfies it
unchanged (the keeper runs one chunk through it), and so does the raw-fetch client in tools/. A
client from P3's transport must provide:

| method | wire | must return | must throw |
|---|---|---|---|
| `info()` | `GET /v1/info` | the SDK's `ServerInfo`: `jobTypes`, and `capabilities[]` of `{jobType, models[{id, installed, resident, vramBytes}]}` | anything; it is relayed as a refusal |
| `upload(blob, {filename})` | `POST /v1/uploads` (multipart, part `file`) | `{blobId}` | |
| `submit(JobRequest)` | `POST /v1/jobs` (`type`, `model`, `params`, `inputs[name].blob_id`, `client_ref`) | the job id | **`CrucibleBusy`** for `409 server_busy` and **`CrucibleLeased`** for `409 leased`, with their `busyLine` / `leasedLine`; **`CrucibleRefused`** (with `status`, `code`, `serverMessage`) for every other refusal. These classes are how the door tells a wait from a failure. |
| `events(jobId)` | `GET /v1/jobs/{id}/events` (SSE) | an async iterable of the SDK's `JobEvent` (warming `{message}`, progress `{fraction, message, extra}`, done `{artifacts, extra}` with the job's own keys, including `primary_stem` and `load_seconds`, under `extra`; failed `{error: {code, message}}`), ending after the first terminal frame | a dropped stream as an error (the door then DELETEs the job and aborts by name) |
| `artifact(jobId, name)` | `GET /v1/jobs/{id}/artifacts/{name}` | the bytes | |
| `cancel(jobId)` | `DELETE /v1/jobs/{id}` | anything | |
| `lease(subject, {act, ttlSeconds})` | `POST /v1/models/{id}/lease` | `{leaseId}` | |
| `heartbeat(leaseId)` / `release(leaseId)` | `POST /v1/leases/{id}/heartbeat`, `DELETE /v1/leases/{id}` | | |

Every request carries the token, `X-Crucible-Api: 1` and `X-Crucible-Client: contentstudio`
(what `/v1/activity` names as the holder). A **work** client must have no per-call deadline: a
deadline would cut off the event stream (client-factory.ts says the same).

The **park** (`ParkOnBusyLane`) is the other injected piece and is P3's to own: called with the
holder's line and the run's abort signal, it resolves when the lane's owner says to ask again.
Until P3 lands, the editor passes `parkOnProbe`: it re-reads the selected server's probe at the
probe's own 15 s cache window until `busyLine` is null. The door is still what decides admission.

Today the editor's client is the P1 factory's SDK `CrucibleClient` on the **selected** server
(`crucibleVoiceIsolation` in denoise.ts, wired in ipc-handlers.ts). Swapping in P3's client is
one line there.

## Checks

All run in this worktree after the last code change:

- `npm run build:all`: exit 0 (electron, preload, frontend).
- `npm run check:pure`: `ALL PASS`.
- `npm run check:crucible`: `check:crucible: ALL PASS (12 keepers)`. The new keeper,
  `test-crucible-denoise.js`, passes 19 of 19 checks. `test-crucible-vendor-pin.js` was updated
  for the module's two new entries.

## Live acceptance on the Mac

Session: `2026-09-23 mic audio.wav` (raw, pcm_s16le, 48 kHz, 3 h 52 m). Chunks of 6 min at
1800, 5400 and 9000 s, extracted as voice_separation.py extracts them (2 ch, 44.1 kHz,
pcm_s24le). The tool is `tools/crucible-denoise-live.js`, and the client is the raw-fetch one.
All runs used the Mac's Crucible only; the PC was never touched.

### Runs

1. **1.0.35, 17:15–18:25 UTC. Parked the whole time, then stopped with SIGINT.** Another
   agent's chaptering run held a lease on qwen3.5-9b and re-took it between its own runs. The
   refusal was `409 leased`, and the row showed "leased: contentstudio-chaptering-run,
   generate, until …". One submit was sent, no job was admitted, and no lease was taken.
2. **1.0.38, 19:06 UTC. Chunk 1800 separated; the stem was then refused, correctly, by
   name.** The server published a **FLAC** stem, `chunk_1800_(vocals)_vocals_mel_band_roformer.flac`,
   where 1.0.34 published WAV. The door read only WAV, so it stopped with
   `voice_isolation_stem_unreadable` and the run ended there. The next commit teaches the door
   to read FLAC as well, and to keep the server's extension; see deviation 7.
3. **1.0.38, 19:09–19:31 UTC, after that fix. Parked and stopped with SIGINT.** A metadata
   run (client `contentstudio`, act `generate`) took a lease on qwen3.8-27b-4bit at 19:08:38,
   three minutes into run 2, and still held it at 19:31. Past the 20 minutes allowed, I stopped
   the run. Nothing was admitted.

So **one real chunk (1800 s) went through Crucible end to end.** Chunks 5400 and 9000 were not
separated on Crucible. Their env references exist, so each needs only the live tool once the
card is free.

### Chunk 1800 on Crucible (job `06a5f3d600654e7bafcc607563ae8bd6`)

| measure | value | source |
|---|---|---|
| wall time, upload to stem on disk | 108.9 s | the tool |
| model load (first chunk of a pass) | 30.8 s | `done.load_seconds` |
| separation | 73.9 s | `done.separate_seconds` |
| input / stem | 15,876,000 frames each, 44.1 kHz, 2 ch; stem FLAC 16-bit | `done` and the door's STREAMINFO check (agrees with ffprobe) |
| memory, declared | 2,523,719,636 B (2.35 GiB) | `/v1/info` `vram_bytes` |
| memory, measured | peak worker RSS 2.97 GB (2,974,777,344 B) | `ps` on the denoise worker, every 2 s, 54 samples |
| card used bytes | 33.4 GB before, 60.7 GB peak | `/v1/accelerator`. Unified memory for the whole machine, and the peak includes the other agent's 27B loading at 19:08, so it is not the model's figure. |
| resident after the run | nothing (`resident: null`, `lease: null`) 3 s after the run ended | `/v1/activity` |
| provenance | `vocals-roformer@929e057b…`, `params: {}` | the artifact's `.provenance.json` |

The env took 116 s on the same chunk while sharing the GPU (101 s and 103 s on the other two),
paying its own model load each time. Crucible's 108.9 s includes a 30.8 s load that a lease
spares the second and later chunks of a pass.

### The null test (chunk 1800)

The script (`nulltest.py`, kept in the session scratchpad and not the repo) decodes both files
with ffmpeg to float and compares them with numpy. Both runs had byte-identical inputs (checked
by comparing the decoded chunks).

| against | alignment | null depth, rms(diff) / rms(ref) | notes |
|---|---|---|---|
| **voice-separator-env on the same chunk** (16-bit WAV stem) | none needed; same length | **−105.7 dB** (diff RMS −133.0 dBFS; peak difference 3.1e-5, one 16-bit LSB) | The same model gives the same stem to within one LSB of quantisation. |
| `mic audio_processed.wav` over the same window (48 kHz, 24-bit, synced) | Crucible stem resampled to 48 kHz and shifted 5,808 samples (0.1210 s; the sidecar says 0.1213 s) | **−37.1 dB** (least-squares gain 0.999) | The residue is the sync's sub-sample shift and the 44.1→48 kHz resample. A gain of 0.999 says no ducking happened here. |

The first alignment in the script had its sign reversed, which gave a +3.0 dB "null", that is,
no alignment at all. Checking both directions gave −37.1 dB at +5,808 samples. The numbers above
use the correct direction.

Levels: raw −27.31 dBFS; env stem, Crucible stem and `_processed` all −27.31 dBFS RMS over this
window, because the mic track is almost all voice.

### Listening clip for Owen

`/Volumes/Callisto/Movies/p7-voice-isolation-acceptance/2026-09-23 mic 00-31-00 raw-env-crucible.wav`
(62 s, 16-bit 44.1 kHz): 20 s of the raw mic from 00:31:00, 1 s of silence, the same 20 s from
voice-separator-env, 1 s of silence, then the same 20 s from Crucible. It lives outside the
session folder so the editor's scanner never sees it.

### Still owed

- Chunks 5400 and 9000 on Crucible: `node tools/crucible-denoise-live.js --input "<mic
  audio.wav>" --at 5400,9000 --seconds 360 --out <dir>`, then `python3 nulltest.py <dir>
  5400,9000`. This also shows the lease holding the separator: the second chunk should report
  `load_seconds` 0.
- A full in-app Process run with Denoise on.

## Deviations from the plan, and why

1. **The editor's client is the SDK's, not P3's transport.** The brief says the transport and
   lanes are another agent's. The seam is typed so the SDK client already fills it, so the editor
   works now instead of waiting on P3; the raw-fetch client exists to prove the seam is not the
   SDK's by accident.
2. **An interim park.** Plan §9 says a busy lane waits with the holder's sentence. With no P3
   lanes yet, `parkOnProbe` waits on the probe (15 s re-reads). It is declared, not a retry loop:
   one submit per release, and the door refuses anything it does not recognise as a wait.
3. **The silent-chunk passthrough follows the stems' codec.** It was hard-coded 16-bit, which
   matched only the local separator's output. The concat demuxer reads every file as the first
   one's codec, so 16-bit silence among 24-bit stems comes out two thirds as long; the keeper's
   end-to-end check caught it. The silence is now written after the loop, in the first real
   stem's codec.
4. **`voice_separation.py` runs in-process** inside `electron_workflow.py` rather than as a
   subprocess: its requests must reach main on the workflow's own stdout, and the env's
   interpreter it used to run under is exactly what nothing may reach now.
5. **`run_audio_separator.py` is deleted** (it only ran inside the env). The env itself and its
   asset-catalog entry stay for P10, as the brief says; the environment modal still lists it.
6. **The module's version changed** to `1.0.34+7de7dfd8ce35` (4 job types, 5 subjects).
7. **A stem is whatever container the server publishes.** The Mac's 1.0.38 sends FLAC where
   1.0.34 sent WAV, and nothing in the job's contract names a container.
   - The door reads a FLAC's STREAMINFO as well as a WAV's header, holds either to the chunk's
     rate and frame count, and writes the stem beside the path Python asked for under the
     server's extension. The answer names the file actually written.
   - Python's silent-chunk passthrough follows the stems' container as well as their codec.
   - The keeper runs the Python end to end with both containers.
   - `readWavFormat`'s refusal is what stopped live run 2 by name instead of shipping an
     unread file. That is Law 1 working, and the reason the fix was needed.

## Open questions for Owen

1. **The rvc env is the price of voice isolation.** The module names `rvc` because `denoise`
   has no installer of its own (plan §19 N3). Should that stay, or should Crucible get a
   denoise-only install?
2. **Busy is a wait, and a leased card can starve it.** Voice isolation is refused for as long as
   any client leases a different resident model, and a run that re-leases between its jobs can
   hold the card for hours, as happened here. Plan §9 says a busy lane waits, but P3's lanes
   decide whether the editor should say so and wait, offer the fast pin, or ask. Owen's call.
3. **Should the environment modal still offer voice-separator-env?** It stays listed until P10
   deletes it. Installing it now downloads 1.2 GB that nothing will run.
4. **Acceptance sign-off by ear.** The clip is at
   `/Volumes/Callisto/Movies/p7-voice-isolation-acceptance/2026-09-23 mic 00-31-00 raw-env-crucible.wav`.
   By the numbers, Crucible's stem is the env's to within one 16-bit LSB (−105.7 dB null).
5. **FLAC stems.** The Mac's 1.0.38 publishes FLAC. Is that the intended contract from now on
   (it halves the download), or should the job document its container? The door accepts
   either way.
