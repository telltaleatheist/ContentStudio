# P9 — the re-roll gate and title ranking (snap)

LEDGER #201 (and Law 3's one declared exception); CRUCIBLE-MIGRATION-PLAN.md §11, §0a, §16 P9.
Built 2026-09-25 on branch `worktree-agent-aaae49b9a35b615ef` off `crucible` (4ba2580).

Owen: *"we could also use snap as a re-roll determiner. if youtube titles dont make sense, if
chapters arent in the right format (they refer to me by name, or 'speaker says', or whatever), it
can send it back for a re-roll. it could also score the titles and order them from best to worst.
everything that scores under 0.5 gets sent back, up to three times."*

**State: built, wired, calibrated; the gate ships ON.** Every live batch ran on 2026-09-25 (the last
ended 23:52 UTC). At the shipped settings the gate agrees with 67 of 70 hand labels (21/23 violators
caught, 1 false alarm in 47). Its false alarms run at about 1 unit in 100 on unseen titles,
thumbnail text, chapter titles and pinned comments, and about 2 in 100 on description sentences.
Title ranking beats chance on the 176 decided A/B tests: 57.0% of pairs against 50%, and 42.0%
first against 33.4%. "The completed calibration" below has the numbers; the earlier sections are
the record of how the statements got there.

## What exists

`electron/services/metadata/reroll/`

| file | what it does |
|---|---|
| `types.ts` | the decide wire (restated, not imported from chaptering), fields, rules, `GateError` |
| `decide-read.ts` | the floor rule, **copied** from `chaptering/assign.ts` (`readYesNo`, `readChoiceDistribution`, `wireOptions`); `check:reroll` holds the copy to the original's answers |
| `rules.ts` | `FIELD_RULES`, the state, one yes/no **statement per rule per unit, quoting the unit**, question names `u<i>_<rule>`, ≤64 questions per decide, the re-roll prompt, the description sentence splitter (byte-exact round trip) |
| `checks.ts` | ask and read; per-rule pass score `1 - max(0, P(yes) - baseline)`; the per-video baseline (median, capped, frozen from the first reading; declared off) |
| `ranking.ts` | a `choice` over ≤26 titles, asked once per **rotation** so a letter/position bias cancels; options named by position; reported as `relative` = p × n (1.0 = a fair share: the per-video baseline) |
| `gate.ts` | per field: score every unit; failing units go back grouped by their first failing rule, one call per rule per round, **at most 3 rounds**; each unit keeps its best-scoring attempt; what still fails ships with a warning naming the rule |
| `settings.ts` | `rerollGate` on/off, `rerollGateTuning` (maxRerolls ≤ 3, thresholds per `<field>.<rule>`, baseline); bad values throw by name |
| `reroll.service.ts` | the binding: fields off the item (link block held back by exact match, chapter titles by position, hook + description sentences), decide on `REROLL_SCORER_MODEL` through `transport.decide` on the GPU lane under the job's leases, re-rolls on the field's own routed model through `runPlainRequest`, record + trace + warnings |

Rules per field (`FIELD_RULES`): titles — creator, narrates, nonsense; chapters — creator, narrates,
sentence, nonsense; description (hook + sentences) — creator, first_person, narrates, nonsense;
thumbnail text — creator, nonsense; pinned comment — creator_third_person, nonsense.

**Wiring.** `metadata-generator.service.ts`: one call after `scrubGeneratedItem`, before the save.
Settings read at job time in the generation IPC (`rerollGate`, `rerollGateTuning`; absent = the
declared defaults). Every decide, every re-roll and the ranking are appended to `_prompt_trace`
with their answers; `reroll_gate` on the item holds the settings, every unit's attempts and
readings, the ranking and the warnings; the warnings also go to the run's. Gate off: the item
records `{mode: 'off'}` and nothing is asked. The reports page shows each generated title's rank
(`#1`…, top three in ink, the relative share in the tooltip) and any rule a title still fails.

**The scorer** is a fixed declared role, `REROLL_SCORER_MODEL = 'qwen3.5-9b'` in
metadata-routing.ts, beside `SUMMARIZATION_MODEL` (not a dialog row: the thresholds were measured on
this model, and no cloud model can answer a decide).

## Calibration (the 9B, Mac Crucible 1.0.38, through `transport.decide`)

Tool: `tools/reroll-calibrate.js` (`rules --set … [--only-labelled] [--rules …]`, `rank`, `report`).
Its own temp registry (one server, the local Crucible; never the app's userData), a lease per batch
of groups, released after; it waits its turn on a busy card. Raw readings are kept in
`docs/crucible/p9-calibration/`. Throughput on the shared card: 1.3–2.8 s per question (P8a
measured 0.65–1.0 alone).

**Hand labels.** `tools/fixtures/titlecheck/labels.jsonl`: 50 chapter titles and 20 descriptions,
sampled with a fixed seed (half from what the scrub, the judges or Owen had touched), labelled
against LEDGER §1 **before** any model read them (commit 2a47061). A description fails a rule when
any of its sentences does.

### Per rule (v1 statements; narrates re-measured as v4)

| rule | P(yes) on violators | on clean | agreement |
|---|---|---|---|
| creator, chapters | 0.73–0.99 (11) | median 0.006, p90 0.026 | 50/50 at P 0.7 |
| creator, descriptions | 0.89–0.98 (4) | median 0.023 | 18/20; the two misses are calls to action ("Subscribe and leave a comment.") |
| first_person, descriptions | 0.73–0.98 (3) | p90 0.029 | 20/20 at P 0.5 |
| narrates **v1**, chapters | 0.68–0.98 | **median 0.73** | 38/50 at best (0.8) — the §0a lean |
| narrates **v2**, chapters | 0.01–0.99 | median 0.03 | 40/50 at best (0.1) — too narrow |
| narrates **v3**, chapters | 0.84–0.99 | median 0.84 | 37/50 — "any verb" |
| narrates **v4**, chapters | 0.41–0.97 | median 0.25 | 44/50 at 0.6; at 0.8 10/16 caught, 1 false alarm |
| narrates v4, descriptions (12 of 20) | 0.89–0.95 (6) | median 0.38 | 12/12 at 0.8 |
| sentence, chapters | 0.04–0.98 (20) | median 0.12 | 42/50 at best (0.8) |
| nonsense | (1 labelled; P 0.02, missed) | p99 0.38 | no garbled unit to measure on |

**The baseline trap (§0a) was real, and was fixed at the source.** v1's narrates statement put a
median P(yes) of 0.73 on CLEAN chapter titles (0.65 on clean description sentences). A per-video
median cannot tell "this model leans" from "this video's titles are all narrated", so instead the
statement was rewritten until clean titles sat low (v4, "the one doing the action is the video
itself or the person presenting it (as in …)"). The baseline machinery is built and keeper-tested
and ships with `baselineCap: 0` (off).

**The gate's verdict** at the declared thresholds (creator P>0.7, first_person P>0.5, narrates
P>0.8, nonsense P>0.5, sentence not gating): **61 of 62** labelled items agree (22/22 violators
caught, 1 false alarm in 40: *"The 'theory in crisis' claim about Darwin and the fossil record,
refuted"*). 8 description labels lack their v4 narrates reading (stopped). The thresholds were
picked on the same 70 labels they are scored on; the corpus run below is what checks them on
unseen text.

### Title ranking over the decided A/B tests

**4 of 177 tests run** before the stop: the winner ranked first in 4/4 and above the loser in 7/7
pairs (mean margin 1.06 in relative share). Four tests are not a result; the rate against chance
(50% per pair, 1/k for first place) and the margin are what the remaining batch measures.

## Deviations from the brief

1. **Re-roll per unit, not per field.** A title, chapter title, description sentence, thumbnail
   text or pinned comment that fails goes back; the rest of its field is untouched. A field's
   "best candidate under the threshold" rule would never re-roll a ten-title list with one bad
   title (the pick-field bar says weak candidates are fine) and would throw away nine good ones
   when it did. Owen's words were "everything that scores under 0.5 gets sent back".
2. **The re-roll is a revision call** (the failing entries in, the same count out, the rule named in
   positive form), not a fresh generation of the whole field: it is the one form in which Law 4
   lets a wrong form be shown, and it keeps the passing entries. It runs on the field's routed
   model with thinking on, as the scrub was measured.
3. **`sentence` is measured but never gates** (threshold 0; Owen, #211).
4. **No whole-text description format rule.** The description is judged sentence by sentence (so a
   failing sentence can be quoted and re-rolled alone); "no first person" is its format rule.
5. **Compilation items are not gated** (the scrub does not cover them either).
6. **Owen's call-to-action ruling is a measured waiver**, not a clause: a question the gate asks (`cta`), because the clause alone did not move the model.

## Owen's answers (LEDGER #211) and what changed

1. **Chapter titles are sentences:** "one or two sentences describing what's being discussed, how
   YouTube expects them to appear in the description". `sentence` never gates (threshold 0).
2. **"X, refuted" / "X, mocked line by line" is a fine format, not narration.** The `narrates`
   statement now says so, with two examples of the format (reroll.yml `rules.narrates`).
3. **Descriptions may carry calls to action**, and a CTA is not a reference to the creator. The
   `creator` statement now says so (`rules.creator`).
4. On all three: "they're coming out fine as is. don't change the prompts." No generation prompt
   changes on the gate's account; only the gate's rule statements and hand labels moved. The blind
   labels already had all six affected items clean on these rules (de09, de13 for creator; ch05,
   ch32, ch34, ch36 for narrates); each now carries `ruled_by: "Owen, LEDGER #211"`.
5. **The ranking threshold** for showing the rank by default is decided when the A/B run is in.

**The two reworded statements are UNMEASURED.** The v1/v4 numbers above are for the wording
before #211; the thresholds stand until the re-measure below.

## The completed calibration (2026-09-25, 22:43–23:52 UTC, the card to itself)

One process at a time, one call in flight, the 9B loaded at 8,192 (#209), a lease per batch.
Raw readings and reports: `docs/crucible/p9-calibration/{remeasure,rank,corpus}/`.

### 1. The two statements #211 reworded, re-measured on the labels

The ", refuted" clause worked: ch05, ch32, ch34 and ch36 now read clean on narrates at P 0.8.
**The call-to-action clause did not:** "Subscribe and leave a comment." still read 0.82 on
creator and 0.91 on narrates, and "Subscribe and tell me what you think in the comments." read
0.53 on first_person. So a call to action is now asked about directly: a `cta` question ("is an
invitation to the viewer, such as to subscribe, comment, share, support the channel or follow a
link") on every description sentence. A YES at `waiverCut` (0.5) WAIVES creator, first_person and
narrates on that sentence, and the reading records `waivedBy: 'cta'`. `cta` read 0.95–1.00 on the
corpus's calls to action and 0.00–0.29 elsewhere. A waiver never fails and never lowers a score.
The narrates reword moved clean chapter titles up (median 0.25 → 0.53) but still separated them at
P 0.8 (12/16 violators on its own; creator catches the rest).

### 2. Title ranking over the decided A/B tests

176 of 177 tests (MUQzRT-nOew has two variants carrying the same title; recorded as skipped).

| | result | chance |
|---|---|---|
| winner above a loser, per pair | **200 / 351 = 57.0%** (z 2.62) | 50% |
| winner ranked first | **74 / 176 = 42.0%** | 33.4% |
| mean margin (winner − loser, share of an even split) | 0.21 (median 0.15; p10 −1.59, p90 1.97) | 0 |

Flat by channel (Unfiltered 54.8% of 31 pairs, Telltale 56.3% of 64, Fireside 57.4% of 256) and by
the size of the watch-time gap (55.6–58.1% across 0–5, 5–10, 10–20 and 20+ points). The ranking
is **above chance, modestly**: it is a guide, not a verdict. Per #211 the threshold for showing the
rank by default is Owen's to set now that this is in; the reports page shows it today as a quiet
`#n` chip with the share in its tooltip. (The z counts pairs, which are not independent within a
three-variant test, so it overstates the confidence somewhat.)

### 3. The corpus, and the thresholds it moved

Samples with the final statements: 60 chapter lists, 60 descriptions, 40 title lists, 40 thumbnail
lists and 40 pinned-comment lists, labelled groups first. On the UNLABELLED groups, at the shipped
settings, each flagged unit read by hand:

| set | units | flagged | read as genuine | read as false alarm |
|---|---|---|---|---|
| chapter titles | 131 | 18 (13.7%) | 17 ("Introducing …", "Discussing …", "the creator calls him …", "The speaker discusses …") | 1 ("Pinochet in Chile, Allende's coalition, and Crowder's helicopter ride jokes", creator 0.78) |
| description sentences | 196 | 25 (12.8%) | ~21 ("This channel …", "I go through …", "We break down …", "The speaker mocks …") | ~4 ("Manley's actual positions …" creator 0.88; "Every argument defaults to Jesus …" narrates 0.92; two "If you searched for …" SEO sentences on first_person) |
| titles | 409 | 4 (1.0%) | 3 ("Owen Morgan takes apart …", "… and Owen calls it what it is") | 1 ("Jack Hibbs says a Portuguese man is not American …", creator 0.84) |
| thumbnail text | 400 | 1 (0.3%) | 0 | 1 ("GOD IS FRIES", nonsense 0.53) |
| pinned comments | 119 | 0 | – | – |

The flag rates on chapters and descriptions are high because many of those reports predate the
scrub (2026-09-04) and really do narrate; the false alarms run at about 1 unit in 100 everywhere
but description sentences (about 2 in 100).

**Three thresholds were moved by this corpus**, so on those three the corpus is no longer unseen
text:
- `description.narrates` fails above P 0.9, not 0.8. At 0.8, subject-first sentences such as "The
  clip brands Angie Nixon …" and "The Fox News framing collapses …" read 0.82–0.85, while every
  labelled description violator read 0.94 or higher.
- `thumbnail_text.creator` fails above 0.9. "SELF-OWN" read 0.87 and "HE HOSTED A RIOTER" 0.84,
  both clean.
- `pinned_comment.creator_third_person` fails above 0.7. The comments are in the creator's voice
  and call the video's subject "he"; four such comments read 0.53–0.62, all clean. There is no
  labelled violator for this rule.

### 4. The verdict, and the default

At the shipped settings, on the 70 hand labels read from this run (final statements, every rule):
**67 of 70 agree; 21 of 23 violators caught, 1 false alarm in 47.** The two misses:
- ch04 "…; the host says race is fake": creator 0.68, just under 0.7.
- de18: the one nonsense label, read 0.02.

The false alarm is ch16 "Reframing 'occupy' as military conquest …" on narrates.

**`rerollGate` defaults ON.** The false-alarm rate held on unseen text, which was your condition.

**Not yet run live: a re-roll call itself.** No unit has yet been sent back to a field model on a
real run. That path is `runPlainRequest` on the field's routed model, the same door the scrub uses,
and `check:reroll` covers it with fakes. The first real generation with the gate on is its first
live exercise. `rerollGate: off` in the store turns the gate off with no build.

## Checks

`npm run check:reroll` (20 checks, no model): quoting not indexing; Owen's #211 standard in the rule statements and the labels; ≤26 options and a 27-title
list declared unranked; floor parity with `chaptering/assign.ts`; the 3-re-roll cap and the
ship-with-warning; best attempt kept; no-evidence passes and is declared; `decide_not_served` by
name; a wrong line count throws; rotations cancel a position bias; on an item: answers by
position, no link block or timestamp in any decision, untouched fields byte-identical, every
decide / re-roll / ranking in `_prompt_trace` with answers and in `reroll_gate` and the warnings;
the splitter round-trips all 571 corpus descriptions; settings refused by name; the gate off asks
nothing.
